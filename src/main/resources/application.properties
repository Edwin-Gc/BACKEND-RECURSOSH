
#spring.datasource.url=${SPRING_DATASOURCE_URL}
#spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
#spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
#spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME}
#spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO}
#spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL}
#spring.jpa.properties.hibernate.dialect=${SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT}
# Conexión a MySQL en AlwaysData
spring.datasource.url=jdbc:mysql://mysql-123.alwaysdata.net:3306/mi_base?useSSL=false&serverTimezone=UTC
spring.datasource.username=426721
spring.datasource.password=*JUEpucha_&(12)-REjeroz_&Ni#

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Configuración del servidor Spring Boot
server.address=0.0.0.0   # usa 0.0.0.0 por defecto
server.port=8080         # usa 8080 por defecto
